/*
var exercises = ["pullups","pushups","situps","jogging","plank","curls"]
var coreExercises = ["situps","plank"]

favExercises.onshow = function() {

    for (i = 0; i < exercises.length; i++) {
        selExercisesLH.addItem(exercises[i])
    }
}
selExercisesLH.onfocusout = function() {}
btnDoneLH.onclick = function() {
    if (selExercisesLH.text[0] == "situps" && selExercisesLH.text[1] == "plank") {
        lblPrompt3LH.value = `You chose ${selExercisesLH.text[0]} and ${selExercisesLH.text[1]} - those are the two core exercises.`
    } else {
        lblPrompt3LH.value = "You did not pick the two core exercises"
    }
}

btnGoTo3LH.onclick = function() {
    ChangeForm(mobileNav)
}
*/